development:
  adapter: async

test:
  adapter: test

local_prod:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: entrii_production
